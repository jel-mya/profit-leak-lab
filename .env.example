# Keep disabled until live Auth/RLS checks and approved project setup are complete.
CLOUD_WORKSPACE_ENABLED=false
# Cloudflare runtime variables. Only a modern publishable key is accepted.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_replace_with_your_public_key
# Local development only; do not commit actual environment files.
