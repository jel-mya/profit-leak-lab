{
  "name": "profit-leak-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:live:writes": "node integration/run-supabase-read-checks.mjs --writes",
    "test:live:reads": "node integration/run-supabase-read-checks.mjs",
    "test:db": "node --test tests/database.test.mjs",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8"
  }
}
